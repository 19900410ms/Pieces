<%= form_with(model: @contribution, local: true) do |form| %>
  <%= form.text_field :title, placeholder: "タイトル" %>
  <%= form.file_field :image, placeholder: "写真" %>
  <%= form.text_area :text, placeholder: "本文" %>
  <%= form.submit "投稿する" %>
<% end %>